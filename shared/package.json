{
  "name": "@hustshawn/dsh-plugin-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared build and test presets for the DSH plugins in this repository",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./tsconfig.plugin.json": "./tsconfig.plugin.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "tsdown": "^0.22.2",
    "vitest": "^3.0.0"
  },
  "devDependencies": {
    "tsdown": "^0.22.2",
    "vitest": "^3.2.7"
  }
}
